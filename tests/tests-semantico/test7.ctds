program {

    integer get_int() extern;
    void print_bool(bool b) extern;

    bool es_positivo(integer n) {
        return n > 0;
    }

    void main() {
        integer valor = get_int();
        bool resultado = es_positivo(valor) && (valor < 100);
        print_bool(resultado);
    }
}