program {

    integer get_int() extern;
    void print_int(integer i) extern;

    void main() {
        integer n = get_int();
        while (n > 0) {
            print_int(n);
            n = n - 1;
        }
    }
}