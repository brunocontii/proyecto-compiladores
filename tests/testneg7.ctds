program {
    
    integer ifelse() extern;
    void whiletrue(integer x) extern;
    
    void main() {
        integer x = 5;
        integer y = 10;
        integer z = 15;
        integer result = x + y;
        
        if (result > 12) then {
            x = resta(10,5);                    // Primer error
            integer temp = result * 2;
            whiletrue(temp);
        } else {
            y = suma (x, y, z);                 // Segundo error
            whiletrue(result);
        }
        
        while (x < y) {
            x = x + 1;
        }
        
        return;
    }
    
    integer suma(integer a, integer b) {
        integer resultado = a + b;
        return resultado;
    }
    
    bool es_par(integer num) {
        integer resto = num % 2;
        return resto == 0;
    }
}