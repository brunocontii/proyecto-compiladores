program {

    integer x = 1;
    integer y = 2;
    integer w = 3;
    integer z = 4;

    integer func(integer a, integer b) {
        integer c = 5;
        integer d = 10;

        if (c > a) then {
            return c;
        }
        if (a > y) then {
            return a;
        } else {
            return 0;
        }
    }

    void print_int(integer x) extern;
    
    void main() {

        /*
        * La variable global "x" y la variable local "d"
        * no están en uso, por ende en la primer pasada
        * deberían ser eliminadas
        */

        z = func(w, 8);
        print_int(z);
    }
}