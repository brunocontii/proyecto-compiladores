program {

    integer suma(integer a, integer b) {
        if (b > 5) then {
            integer a = 5;
        }
        return a + b;
    }

    void main() {
        integer x = 0;
        x = suma(x, 6);
        return;
    }

}