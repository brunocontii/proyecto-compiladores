program {
    integer x = 5;
    
    integer foo(integer a, bool b) {
        if(a > 0) then {
            return a;
        }

        integer y = 10; // en un bloque primero va declaraciones luego statements
        
        return y;
    }

    void main() {
        integer x = 10;
    }
    
}