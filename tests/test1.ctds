program {
        
    void print_int(integer a, integer b, integer c, integer d) {
        return;
    }
    
    void main() {
        integer y = 0;
        if (y == 1) then {
            bool e = true;
            integer f = 0;
            integer g = 0;
            integer h = 0;
            print_int(e, f, g, h);
        } else {
            
        }
    }
}